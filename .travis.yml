dist: focal
group: dev
vm:
  size: 'gpu-medium'
script:
  - nvidia-smi
  - sudo apt-get update
  - sudo apt-get install -y nvidia-cuda-toolkit gcc-6
  - sudo apt-get install -y cuda-samples-10-1
  - cd /usr/local/cuda-11.0/samples/1_Utilities/deviceQuery && sudo make && ./deviceQuery
